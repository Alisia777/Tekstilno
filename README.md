# Tekstilno Portal v26 Focus Assignments

Что добавлено в этой сборке:

- Взята **красная заливка Вартана** из файлов:
  - `СВОД Маржинальность АПРЕЛЬ 2026 WB (1).xlsx`
  - `СВОД Маржинальность АПРЕЛЬ 2026 OZON (1).xlsx`
- Для менеджеров сформирован **закреплённый пул артикулов в работе**:
  - `WB / Анастасия` — 63 SKU
  - `Ozon / Ирина Паламарук` — 37 SKU
- Пул разбит на **пакеты по 20 SKU** и больше не крутится по кругу.
- На каждый артикул добавлены:
  - сигнал из файла (`Кол продаж`, `Отменили`, `% выкупа` и т.д.)
  - дата старта пакета
  - срок исполнения пакета
  - базовый 5-дневный план действий
- В задачах менеджеров теперь видно:
  - `WB арт.`
  - `Ozon арт.`
  - `сигнал`
  - `срок`
  - краткий план работ

## Что лежит в архиве

- `index.html` — основной портал
- `portal.css`, `portal.js` — основной контур
- `nina.html`, `nina.css`, `nina.js` — матрица кластеров и поставок
- `shared-storage.js` — слой хранения preview / backend
- `config.js` — конфигурация
- `data/` — план, история, матрица Нины, demo reports
- `backend/` — SQL и инструкция для Supabase
- `exports/` — выгрузки закреплённых артикулов для проверки

## Как обновить

1. Заменить **репозиторий целиком** этой сборкой.
2. Не удалять `CNAME`, если он уже лежит в репозитории.
3. После публикации сделать `Ctrl+F5`.
4. Проверить:
   - `/`
   - `Задачи менеджеров`
   - `WB арт.`
   - `Сигнал`
   - `Срок`
   - `/nina.html`

## Хранение отчётности

В этой сборке по умолчанию всё ещё включён `session-preview`.
Чтобы статусы, комментарии и заявки были видны всем:

1. Выполнить SQL из `backend/portal_shared_storage_supabase.sql`.
2. В `config.js` переключить:
   - `backend.provider = "supabase"`
   - заполнить `backend.supabaseUrl`
   - заполнить `backend.supabaseAnonKey`

## Примечания по WB

- До `27.04.2026` на WB действует бонусный рекламный бюджет — высокий расход рекламы сам по себе не блокирует работу по артикулу.
- `20.04.2026` меняется комиссия WB — перед этой датой стоит перепроверить экономику и отключение доп. функций.
