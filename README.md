# Текстильно · v17

Что исправлено:
- устранена ошибка `Cannot read properties of undefined (reading 'includes')` — причина была в смешанной сборке, когда старый `index.html/app.js` остались, а `config.js` был уже от новой частичной версии;
- корневой экран снова рабочий: менеджеры и руководитель открываются без ошибки загрузки;
- вкладка `Поставки` больше не ведет в узкий сломанный блок — теперь она сразу открывает полноэкранную страницу `nina.html`;
- добавлена полная конфигурация и все нужные данные для основного сайта:
  - `data/article-plan.json`
  - `data/cluster-reference.json`
  - `data/history-data.json`
- в корневом экране добавлены быстрые кнопки WB / Ozon / Руководитель / Матрица Нины.

Что заменить в репозитории:
- весь архив целиком, а не отдельные файлы.

После заливки:
1. дождаться обновления GitHub Pages;
2. сделать жесткое обновление `Ctrl+F5`;
3. открыть:
   - `/` — основной экран;
   - `/nina.html` — полноэкранная матрица кластеров.
